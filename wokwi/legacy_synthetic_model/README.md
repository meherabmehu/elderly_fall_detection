# Wokwi

Open this folder in VS Code with PlatformIO and Wokwi Simulator. Build before starting the simulator. The included `src/main.cpp` runs the KFall S06T20R01 replay (`REPLAY_MODE 1`). Set it to `0` for live MPU6050 input.
